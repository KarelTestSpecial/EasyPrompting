# Easy Prompting

A safe scratchpad for writing AI chat prompts. Draft your prompt in a small
window that stays open beside your chat, never lose it to a stray **Enter** or
**Ctrl+C**, then copy it over when it's ready.

## Why

Many AI chatbots and agent chat interfaces lose your in-progress prompt far too
easily: a wrong key in a terminal/TUI (e.g. `Ctrl+C`), or hitting **Enter** when
you meant **Shift/Alt/Ctrl+Enter**, sends or wipes what you were writing. Easy
Prompting gives you a calm place to write first. Nothing is ever sent from this
window — **Enter just makes a new line**.

## Usage

Click the toolbar icon to open the pad; click it again to close it. The window
stays open while you work in another tab, so you can draft here and paste into
your chat. Everything you type is saved automatically.

- **Copy** — one click copies the current tab to the clipboard.
- **Tabs** — keep several prompts side by side (a system prompt, a draft, some
  snippets). Click `+` to add, `×` to delete, double-click a tab to rename.
- **Clear** — empties the current tab (with confirmation).
- **A- / A+** — adjust text size.

### Keyboard shortcuts (while typing)
- **Alt + E / Alt + R** — previous / next tab.
- **Alt + 1 … 9** — jump straight to a tab.
- **Alt + ← / →** or **Ctrl + [ / ]** — previous / next tab (alternatives).

## Privacy

100% local. Drafts and settings are stored on your device via `chrome.storage`
and are never transmitted. See `LEGAL/PRIVACY.md`.

## Related

Need a clock, alarms, screensaver and richer notes too? See the sibling
extension **Very Practical Clock & Notepad**.

## Development

Unpacked MV3 extension. Load `easyprompting/` via `chrome://extensions` →
*Load unpacked*. Icons are generated by `make_icons.py` (requires Pillow).
